exports.transcribe = function(path, cb)
{
  //TODO Insert transcription process here
  //
  // If successful, run
  // cb(null, transcript, segments);
  //
  // otherwise, run
  // cb('Speech-to-text failed at upload stage.', undefined, undefined);
  
  cb('Speech-to-text process missing. Please see README for setup instructions.', undefined, undefined);
};
